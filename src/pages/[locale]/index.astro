---
import HomePage from "../../components/HomePage.astro";

export function getStaticPaths() {
    return [
        { params: { locale: "hu" } },
        { params: { locale: "de" } },
        { params: { locale: "en" } },
    ];
}
---

<HomePage />
